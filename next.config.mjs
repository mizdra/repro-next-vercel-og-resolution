/** @type {import('next').NextConfig} */
const nextConfig = {
  // webpack(config, ctx) {
  //   console.log(ctx.isServer, ctx.nextRuntime, config)
  //   return config;
  // }
};

export default nextConfig;
